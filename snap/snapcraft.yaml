name: pypy3
version: '7.2.0'
summary: PyPy is a fast, compliant alternative implementation of the Python language
description: |
  PyPy is a replacement for CPython. It is built using the RPython language
  that was co-developed with it. The main reason to use it instead of CPython
  is speed; it runs generally faster.
license: MIT

grade: stable
confinement: classic

apps:
  pypy3:
    command: bin/pypy3

base: core
architectures:
  - build-on: amd64

parts:
  pypy3:
    plugin: dump
    source: https://bitbucket.org/pypy/pypy/downloads/pypy3.6-v$SNAPCRAFT_PROJECT_VERSION-linux64.tar.bz2
    override-build: |
      snapcraftctl build
    build-packages:
      - tcl-dev
      - tk-dev
    stage-packages:
      - libfontconfig1
      - libtcl8.6
      - libtk8.6
      - libx11-6
      - libxau6
      - libxcb1
      - libxdmcp6
      - libxext6
      - libxft2
      - libxrender1
      - libxss1
      - ncurses-base
